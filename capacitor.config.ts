import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.octokit.web',
  appName: 'github-web-octokit',
  webDir: 'dist'
};

export default config;
